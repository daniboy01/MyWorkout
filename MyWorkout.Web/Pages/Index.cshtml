@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="row">
    @foreach(var category in Model.CategoryHeaders)
     {
    <div class="col-md-4">
        <a asp-page="/WorkoutPlans/Details" asp-route-id="@category.Id">
            <img src="~/images/workoutPlan_covers/@(category.Id).jpg" />
        </a>
    </div>           
     }
</div>
